[project]
name = "peak-divergence"
version = "0.1.0"
description = "Peak-Divergence Game multi-agent quality-diversity benchmark."
requires-python = ">=3.11"
dependencies = ["numpy>=1.24"]

[tool.ruff]
line-length = 100
